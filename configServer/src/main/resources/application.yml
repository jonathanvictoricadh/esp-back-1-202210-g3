server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jonathanvictoricadh/esp-back-1-202210-g3-config
          default-label: circuitBreak
          force-pull: true
